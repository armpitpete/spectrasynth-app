# v0.24 Stable Spectral Band State Checkpoint

This note records the v0.24 checkpoint for SpectraSynth.

## Status

The tested v0.23 spectral band state preparation is frozen.

No sound behaviour changes are intended in v0.24.

## Preserved audio path

```text
oscillator / noise → pre-fuzz low-pass filter → soft Buttery Fuzz mix → post-fuzz low-pass filter → true left/right stereo spread → master Output → analyser / speakers
```

## Preserved spectral panel behaviour

The 10 spectral bands store:

```text
band number
label
fader value
muted state
analyser level
```

The analyser meters still respond to real output audio.

The band faders remain visual-only.

The Mute buttons remain visual-only.

## Check

- header says `v0.24 stable spectral band state checkpoint`
- oscillator works
- noise works
- analyser meters still respond to real audio
- moving each band fader updates the patch summary
- moving band faders does not change sound
- Mute buttons visually toggle band state
- Mute buttons do not change sound
- Cutoff / Brightness still works across the full slider
- Buttery Fuzz remains soft and rounded
- Output works
- Panic Stop works
- Feedback remains parked
