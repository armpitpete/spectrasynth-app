# v0.54 — Active control wording correction

Scope: wording only.

Goal: stop the app summary from describing active movement/effects controls as unconnected.

Corrected wording:

- Virtual Distance is active.
- Two-Moon Movement is active.
- Delay is active.
- Cathedral Reverb is active.
- Band 5 audio filtering remains not connected.
- The all-10-band filter bank remains not connected.
- Feedback remains not connected.
- Vocoder, microphone, MIDI, sensor input, band-fader audio behaviour, and fake self-oscillation remain not connected.

Implementation:

- added `src/active-control-wording.js`
- loaded it after active movement/effects modules
- it corrects the Plain Patch Summary text after `main.js` updates it

Preserved:

- no audio logic changes
- no Web Audio routing changes
- no gain constant changes
- no CSS changes
- no Delay behaviour changes
- no Reverb behaviour changes
- no Band 5 audition
- no feedback loop
- no filter-bank behaviour
- no deployment workflow changes

Test checklist:

- app loads
- Plain Patch Summary says Virtual Distance, Two-Moon Movement, Delay, and Cathedral Reverb are active
- Plain Patch Summary no longer says delay/reverb effects are not connected
- Delay still works
- Reverb still works
- Panic Stop still works
